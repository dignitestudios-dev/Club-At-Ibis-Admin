import CategoryBuilder from "@/features/categories/components/category-builder";

export const metadata = { title: "Add Category · Club At Ibis Admin" };

export default function Page() {
  return <CategoryBuilder />;
}
